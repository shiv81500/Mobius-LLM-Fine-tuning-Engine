package com.llmtrainer.model;

/**
 * Enum representing supported training data file formats.
 */
public enum FileFormat {
    JSONL,  // JSONL format (one JSON object per line)
    CSV,    // CSV format (tabular data)
    TXT     // Plain text format
}
